{%- liquid assign section_id = section.id -%}

<section id="section-{{ section_id }}" class="footer-section" data-section-id="{{ section_id }}">
  <div class="footer">
    <!-- o container que o JS vai usar: -->
    <div class="bubbles" data-bubbles="{{ section.settings.bubble_count | default: 128 }}"></div>

    <div class="content">
      <!-- seu conteúdo (colunas, links, logo) -->
      <div>
        <!-- ... -->
      </div>

      <div>
        <a class="image" href="#" style="background-image: url('{{ 'happy.svg' | asset_url }}')"></a>
        <p>©2019 Not Really</p>
      </div>
    </div>
  </div>
</section>
